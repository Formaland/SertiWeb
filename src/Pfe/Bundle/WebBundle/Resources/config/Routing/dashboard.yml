pfe_web_homepage:
    pattern:  /
    defaults:
        _controller: "FrameworkBundle:Redirect:redirect"
        route: pfe_web_dashboard
        permanent: true

pfe_web_dashboard:
    pattern:  /dashboard
    defaults: { _controller: PfeWebBundle:Dashboard:index }