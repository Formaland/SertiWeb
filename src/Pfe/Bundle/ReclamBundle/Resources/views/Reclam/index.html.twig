{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reclam list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Reclamref</th>
                <th>Reclamdate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('reclam_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.reclamRef }}</td>
                <td>{% if entity.reclamDate %}{{ entity.reclamDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('reclam_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('reclam_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('reclam_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
